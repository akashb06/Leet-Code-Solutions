class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        j=[]
        for i in (nums):
           j.append(i)
            
        for i in (nums):
           j.append(i)
        
        return j
